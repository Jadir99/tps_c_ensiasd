#include <stdio.h>

int main(){
    // programmes qui calcule le (a+b)^2
    int a,b;
    printf("donner les valeurs de a est b :");
    scanf("%d%d",&a,&b);

    printf(" le valeur de (a+b)^2 est %d",(a+b)*(a+b),(a+b)*(a+b));
    return 0;
}