#include <stdio.h>

int main(){
    // la mention 


    int note;

    if (note>=10 && note <12)
        printf("PASSABLE");
        else if(note>=12 && note<14)
        printf("assez bien ");
            else if(note>=14 && note<16)
            printf("BIEN");
                else if(note>=16 && note<=20);
                printf("tres bien");
    return 0;
}